/**
 * cart.js - Deprecated
 * 
 * NOTE: The old `haven_global_cart` system has been transitioned to the unified `hc_cart_items` standard.
 * Logic is now directly implemented within `cart.html`, `auth.js` (`syncCartBadge`), and `shared.js` (`addToCart`).
 */
